checks:
  php: true
filter:
  paths:
    - 'src/*'
  dependency_paths:
    - 'vendor/*'

build:
  image: default-jammy
  nodes:
    analysis:
      environment:
        php:
          version: 7.4
      tests:
        override:
          - php-scrutinizer-run
